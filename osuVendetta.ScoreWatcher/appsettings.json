{
  "ScoreWatcher": {
    /* How long should we wait before fetching new scores if we previously didn't receive any new ones
       Recommended: 1000 */
    "DelayIfNoScoresMs": 1000,
    // How many scores (with replays) we should cache from each scores fetch
    "ScoresToCache": 50,
    // How long to wait between downloads
    "DelayBetweenDownloadsMs": 5500
  },
  // See: https://osu.ppy.sh/home/account/edit under section 'OAuth'
  "OsuApi": {
    // Api v2 client id
    "ClientId": 0,
    // Api v2 client token
    "ClientToken": ""
  },
  "DiscordWebhook" : {
    "WebhookUri": ""
  }
}